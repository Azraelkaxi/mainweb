# frontend

个人主页前端。Vite + React + TypeScript。

```bash
npm install
npm run dev
npm run build
```

`/api` 会代理到本机 `127.0.0.1:3001`，留给之后的 backend。

视觉模板在仓库根目录 `STYLE.md`。token 在 `src/styles/`，可复用组件在 `src/components/`。
